# This file was generated automatically from conda-smithy. To update this configuration,
# update the conda-forge.yml and/or the recipe/meta.yaml.
# -*- mode: yaml -*-

name: Build conda package
on:
  push:

  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.CONFIG }}
    runs-on: ${{ matrix.runs_on }}
    timeout-minutes: 360
    strategy:
      fail-fast: false
      max-parallel: 50
      matrix:
        include:
          - CONFIG: linux_64_cuda_compiler_version12.9python3.11.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9python_h9224ed27
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_version12.9python3.12.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9python_h33f95ef4
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_version12.9python3.13.____cp313
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9python_hd2594ab6
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_version12.9python3.14.____cp314
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version12.9python_hf4dae836
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_version13.0python3.11.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version13.0python_h18013c65
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_version13.0python3.12.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version13.0python_h61e10ca6
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_version13.0python3.13.____cp313
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version13.0python_hb5a75eed
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_version13.0python3.14.____cp314
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_version13.0python_h4ed4b096
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_versionNonepython3.11.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_versionNonepython_h3c1a96fc
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_versionNonepython3.12.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_versionNonepython_h7883cd14
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_versionNonepython3.13.____cp313
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_versionNonepython_h46731b07
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_64_cuda_compiler_versionNonepython3.14.____cp314
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_cuda_compiler_versionNonepython_ha12e3867
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-latest']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version12.9python3.11.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9p_hc87dfb16
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version12.9python3.12.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9p_he8413cac
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version12.9python3.13.____cp313
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9p_h09be3c5c
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version12.9python3.14.____cp314
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version12.9p_hbed3ff9c
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version13.0python3.11.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version13.0p_heba4f3b6
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version13.0python3.12.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version13.0p_ha3bffc4c
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version13.0python3.13.____cp313
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version13.0p_h115c0710
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_version13.0python3.14.____cp314
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_version13.0p_hfe4d483b
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_versionNonepython3.11.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_versionNonep_h61f3f778
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_versionNonepython3.12.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_versionNonep_h72ca1b73
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_versionNonepython3.13.____cp313
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_versionNonep_h35b9d921
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: linux_aarch64_cuda_compiler_versionNonepython3.14.____cp314
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_cuda_compiler_versionNonep_h78b29237
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['ubuntu-24.04-arm']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-aarch64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
            docker_run_args: 
          - CONFIG: osx_arm64_python3.11.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: osx_arm64_python3.11.____cpython
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['depot-macos-15']
            tools_install_dir: ~/miniforge3
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
          - CONFIG: osx_arm64_python3.12.____cpython
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: osx_arm64_python3.12.____cpython
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['depot-macos-15']
            tools_install_dir: ~/miniforge3
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
          - CONFIG: osx_arm64_python3.13.____cp313
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: osx_arm64_python3.13.____cp313
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['depot-macos-15']
            tools_install_dir: ~/miniforge3
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
          - CONFIG: osx_arm64_python3.14.____cp314
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: osx_arm64_python3.14.____cp314
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['depot-macos-15']
            tools_install_dir: ~/miniforge3
            build_workspace_dir: ~/miniforge3/conda-bld
            docker_run_args: 
    steps:

    - name: Manage disk space
      if: matrix.os == 'ubuntu'
      run: |
        sudo mkdir -p /opt/empty_dir || true
        for d in \
                 /opt/ghc \
                 /opt/hostedtoolcache \
                 /usr/lib/jvm \
                 /usr/local/.ghcup \
                 /usr/local/lib/android \
                 /usr/local/share/powershell \
                 /usr/share/dotnet \
                 /usr/share/swift \
                 ; do
          sudo rsync --stats -a --delete /opt/empty_dir/ $d || true
        done
        BROWSERS="firefox google-chrome-stable microsoft-edge-stable"
        BROWSERS_TO_REMOVE=$(dpkg --get-selections $BROWSERS 2>/dev/null | awk '{print $1}')
        sudo apt-get remove --purge -y $BROWSERS_TO_REMOVE

        sudo apt-get autoremove -y >& /dev/null
        sudo apt-get autoclean -y >& /dev/null
        df -h

    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

    - name: Build on Linux
      id: build-linux
      if: matrix.os == 'ubuntu'
      env:
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONFIG: ${{ matrix.CONFIG }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        DOCKER_IMAGE: ${{ matrix.DOCKER_IMAGE }}
        CI: github_actions
        CONDA_FORGE_DOCKER_RUN_ARGS: "${{ matrix.docker_run_args }}"
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
        FEEDSTOCK_TOKEN: ${{ secrets.FEEDSTOCK_TOKEN }}
        STAGING_BINSTAR_TOKEN: ${{ secrets.STAGING_BINSTAR_TOKEN }}
      shell: bash
      run: |
        if [[ "$(uname -m)" == "x86_64" ]]; then
          echo "::group::Configure binfmt_misc"
          docker run --rm --privileged multiarch/qemu-user-static:register --reset --credential yes
        fi
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="$(basename $GITHUB_REF)"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        echo "::endgroup::"
        ./.scripts/run_docker_build.sh

    - name: Build on macOS
      id: build-macos
      if: matrix.os == 'macos'
      env:
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONFIG: ${{ matrix.CONFIG }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        CI: github_actions
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
        FEEDSTOCK_TOKEN: ${{ secrets.FEEDSTOCK_TOKEN }}
        STAGING_BINSTAR_TOKEN: ${{ secrets.STAGING_BINSTAR_TOKEN }}
      shell: bash
      run: |
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="$(basename $GITHUB_REF)"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        ./.scripts/run_osx_build.sh

    - name: Build on windows
      id: build-windows
      if: matrix.os == 'windows'
      shell: cmd
      run: |
        set "flow_run_id=github_%GITHUB_RUN_ID%"
        set "remote_url=https://github.com/%GITHUB_REPOSITORY%"
        set "sha=%GITHUB_SHA%"
        call ".scripts\run_win_build.bat"
      env:
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        PYTHONUNBUFFERED: 1
        CONFIG: ${{ matrix.CONFIG }}
        CI: github_actions
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
        FEEDSTOCK_TOKEN: ${{ secrets.FEEDSTOCK_TOKEN }}
        STAGING_BINSTAR_TOKEN: ${{ secrets.STAGING_BINSTAR_TOKEN }}

    - name: Determine build outcome
      # this is to merge the status of the linux/osx/win builds into
      # something we can easily reuse during artefact generation
      id: determine-status
      if: ${{ always() }}
      shell: bash
      env:
        OS: ${{ matrix.os }}
      run: |
        if [[ "$OS" == "ubuntu" ]]; then
          STATUS=${{ steps.build-linux.outcome }}
        elif [[ "$OS" == "macos" ]]; then
          STATUS=${{ steps.build-macos.outcome }}
        elif [[ "$OS" == "windows" ]]; then
          STATUS=${{ steps.build-windows.outcome }}
        fi
        if [ -z "$STATUS" ]; then
          # steps that never ran will have empty status
          STATUS="cancelled"
        fi
        echo "status=$STATUS" >> $GITHUB_OUTPUT

    - name: Prepare conda build artifacts
      continue-on-error: true
      id: prepare-artifacts
      shell: bash
      # we do not want to trigger artefact creation if the build was cancelled
      if: ${{ always() && steps.determine-status.outputs.status != 'cancelled' && matrix.STORE_BUILD_ARTIFACTS }}
      env:
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CI: github_actions
        CONFIG: ${{ matrix.CONFIG }}
        SHORT_CONFIG: ${{ matrix.SHORT_CONFIG }}
        JOB_STATUS: ${{ steps.determine-status.outputs.status }}
        OS: ${{ matrix.os }}
      run: |
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        export CI_RUN_ID=$GITHUB_RUN_ID
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export ARTIFACT_STAGING_DIR="$GITHUB_WORKSPACE"
        # Archive everything in CONDA_BLD_PATH except environments
        # Archive the CONDA_BLD_PATH environments only when the job fails
        # Use different prefix for successful and failed build artifacts
        # so random failures don't prevent rebuilds from creating artifacts.
        if [ $JOB_STATUS == "failure" ]; then
          export BLD_ARTIFACT_PREFIX="conda_artifacts"
          export ENV_ARTIFACT_PREFIX="conda_envs"
        else
          export BLD_ARTIFACT_PREFIX="conda_pkgs"
        fi
        if [ $OS == "windows" ]; then
          pwsh -Command ". '.scripts/create_conda_build_artifacts.bat'"
        else
          ./.scripts/create_conda_build_artifacts.sh
        fi

    - name: Store conda build artifacts
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
      if: ${{ always() && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true

    - name: Store conda build environment artifacts
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
      # only relevant if build failed, see above
      if: ${{ always() && steps.determine-status.outputs.status == 'failure' && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true